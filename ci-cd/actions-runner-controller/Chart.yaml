apiVersion: v2
name: actions-runner-controller
description: GitHub Actions Runner Controller (gha-runner-scale-set-controller) on mgmt-workload — controller-only; per-repo runner scale sets are deployed separately.
type: application
version: 0.1.0
appVersion: "0.9.3"

dependencies:
  - name: gha-runner-scale-set-controller
    version: "0.9.3"
    repository: oci://ghcr.io/actions/actions-runner-controller-charts
