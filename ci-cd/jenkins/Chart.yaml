apiVersion: v2
name: jenkins
description: Jenkins on mgmt-control — official jenkinsci Helm chart
type: application
version: 0.1.0
appVersion: "LTS"

dependencies:
  - name: jenkins
    version: "5.9.18"
    repository: https://charts.jenkins.io
