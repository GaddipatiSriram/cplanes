apiVersion: v2
name: sonarqube
description: SonarQube on mgmt-forge — sonarsource's official chart for SonarQube + a tiny in-chart postgres Deployment using upstream postgres:17 (no bitnami). Ingress at sonarqube.apps.mgmt-forge.engatwork.com.
type: application
version: 0.4.0
appVersion: "2026.2.1"

dependencies:
  - name: sonarqube
    version: "2026.2.1"
    repository: https://SonarSource.github.io/helm-chart-sonarqube
