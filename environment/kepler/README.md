# kepler

📋 **Placeholder** — chart not yet authored.

Planned location for the `kepler` Helm chart, values, and supporting templates.

When work begins:

1. Add `Chart.yaml` (umbrella over upstream chart, or self-contained)
2. Add `values.yaml`
3. Add the AppSet / Application manifest at `../../argo-applications/sre/environment/kepler-appset.yaml`

See `../README.md` for plane context and `../todo.md` for sequencing.
