# rabbitmq-operator

📋 **Placeholder** — chart not yet authored.

Planned location for the `rabbitmq-operator` (RabbitMQ Cluster Operator from VMware/cloudnative-rabbitmq) Helm chart. Manages RabbitMQ clusters declaratively via the `RabbitmqCluster` CRD.

When work begins:

1. Add `Chart.yaml` (umbrella over upstream `rabbitmq-cluster-operator` chart, or self-contained)
2. Add `values.yaml`
3. Add the AppSet / Application manifest at `../../argo-applications/sre/data/rabbitmq-operator-appset.yaml`

See `../README.md` for domain context and `../todo.md` for sequencing.
