# cloudnative-pg

📋 **Placeholder** — chart not yet authored.

Planned location for the `cloudnative-pg` Helm chart, values, and supporting templates.

When work begins:

1. Add `Chart.yaml` (umbrella over upstream chart, or self-contained)
2. Add `values.yaml`
3. Add the AppSet / Application manifest at `../../argo-applications/sre/data/cloudnative-pg-appset.yaml` (or matching directory)

See `../README.md` for domain context and `../todo.md` for sequencing.
