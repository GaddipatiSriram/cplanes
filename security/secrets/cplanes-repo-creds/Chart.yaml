apiVersion: v2
name: cplanes-repo-creds
description: ExternalSecret CRs that render the ArgoCD repo-cred Secret `cplanes-repo` in argocd-sre + argocd-devops namespaces, sourced from Vault path `kv/forge/github` (key `pat`). Replaces the kubectl-applied plaintext PAT bootstrap.
type: application
version: 0.1.0
appVersion: "1.0"
