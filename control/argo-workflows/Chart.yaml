apiVersion: v2
name: argo-workflows
description: Argo Workflows on mgmt-control — workflow engine for CI/CD and batch DAGs
type: application
version: 0.1.0
appVersion: "3.6.2"

dependencies:
  - name: argo-workflows
    version: "0.42.0"
    repository: https://argoproj.github.io/argo-helm
