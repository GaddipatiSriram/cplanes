# ApplicationSet — Argo Rollouts on every role=control cluster.
#
# Sync wave 2 — alongside other control-plane services. Argo Rollouts
# is operationally adjacent to ArgoCD (which is wave -10), so it lands
# after the GitOps controller is up.

apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: argo-rollouts
  namespace: argocd-sre
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    - clusters:
        selector:
          matchLabels:
            role: control
  template:
    metadata:
      name: 'argo-rollouts-{{.name}}'
      labels:
        app.kubernetes.io/name: argo-rollouts
        app.kubernetes.io/part-of: control-plane
      annotations:
        argocd.argoproj.io/sync-wave: "2"
    spec:
      project: default
      source:
        repoURL: https://github.com/GaddipatiSriram/cplanes.git
        targetRevision: HEAD
        path: control/argo-rollouts
        helm:
          releaseName: argo-rollouts
      destination:
        server: '{{.server}}'
        namespace: argo-rollouts
      syncPolicy:
        automated:
          selfHeal: true
          prune: true
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
          - SkipDryRunOnMissingResource=true
          - RespectIgnoreDifferences=true
        retry:
          limit: 5
          backoff:
            duration: 10s
            factor: 2
            maxDuration: 3m
      ignoreDifferences:
        - group: apiextensions.k8s.io
          kind: CustomResourceDefinition
          jqPathExpressions:
            - .spec.conversion.webhook.clientConfig.caBundle
